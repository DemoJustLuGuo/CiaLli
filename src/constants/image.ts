/**
 * 响应式图片和裁剪相关常量。
 *
 * 集中管理图片断点宽度、裁剪质量等配置，
 * 供 ResponsiveImage.astro、相册页、裁剪模态框等共享使用。
 */

// ---------------------------------------------------------------------------
// 响应式图片断点宽度
// ---------------------------------------------------------------------------

/** 相册瀑布流布局的最大图片宽度 */
export const ALBUM_MASONRY_WIDTH = 1920;

/** 相册网格布局的图片宽度 */
export const ALBUM_GRID_WIDTH = 1200;

/** 相册网格布局的图片高度 */
export const ALBUM_GRID_HEIGHT = 900;

// ---------------------------------------------------------------------------
// JPEG 裁剪输出质量梯度
// ---------------------------------------------------------------------------

/**
 * JPEG 压缩质量递减梯度。
 * 裁剪输出超过大小限制时，依次尝试更低质量直到满足要求。
 */
export const JPEG_QUALITY_STEPS = [0.9, 0.82, 0.75] as const;
